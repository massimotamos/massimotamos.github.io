# Site settings
title: Massimo Tamos' Blog
description: 'Software and Security Engineer. <br />Lives in Zürich. <br />A Cyber Security enthusiast, Security Engineering Msc, MBA, PMP, CISSP, CCSP, CEH.
'
baseurl: ''
google_analytics: 'UA-159974639-1'
disqus_shortname: 'blupadmamax@gmail.com'
URL: 'massimotamos.github.io'

author:
  name: 'Massimo Tamos'
  email: massimo.tamos@hotmail.com
  twitter_username:
  facebook_username: massimotamos
  github_username:  massimotamos
  linkedin_username:  massimotamos
  pgpkey: 'https://pgp.circl.lu/pks/lookup?op=get&search=0xC8'

defaults:
  -
    scope:
      path: ''
      type: 'posts'
    values:
        layout: 'post'

# Build settings
destination: _site
paginate: 10
permalink: /:year/:title/
markdown: kramdown
highlighter: rouge
environment: production

sass:
    sass_dir: _sass
    style: compressed

kramdown:
  # use Github Flavored Markdown
  input: GFM
  # do not replace newlines by <br>s
  hard_wrap: false

plugins: ['jekyll-paginate','jekyll-seo-tag', 'jekyll-redirect-from']
exclude: ['README.md', 'Gemfile', 'Gemfile.lock', 'screenshot.png']
include: ['.well-known', 'robots.txt']

